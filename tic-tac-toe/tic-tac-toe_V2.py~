'''
Py game testing
'''

import os

class check_folder:

        ''' this is a simple program to create subfolders in current dirs'''
        def __init__(self):
                print "initalized"

        def check_exists(self): 
                self.creation = "tic-tac-toe.py"
                self.current_path = os.getcwd
                self.current_files = os.listdir(os.curdir)
                if self.creation in self.current_files:
                        return 0
                else:
                        '''Make dirs from list'''


'''How to call class'''
#folder_exists = check_folder()
#true = folder_exists.check_exists()
#print true
